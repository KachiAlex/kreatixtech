{
  "version": 2,
  "buildCommand": "npx prisma generate --schema=prisma/schema.prisma && npx prisma db push --schema=prisma/schema.prisma --accept-data-loss",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index" }
  ]
}
